/**
    Rust Bootcamp Week One: Day One
    - Setting Up the Environment
    - Install Rust and set up the development environment.
    - Basic command-line usage and first "Hello, World!" program
*/

// This function serves as the entry point to this module.
// In rust, everything in modules are private by default,
// use the `pub` keyword to make them visible outside the module.
pub fn main() {
    println!("Hello world!");
    gm_to_rust();
}

// function to print GM Rust!
fn gm_to_rust() {
    println!("GM Rust!");
}
